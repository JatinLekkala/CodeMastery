{
  "name": "codemastery-root",
  "version": "1.0.0",
  "description": "Root orchestration for Codemaster Competitive Coding Platform",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:worker": "npm run worker --prefix backend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:worker\" \"npm run dev:frontend\"",
    "start:backend": "node backend/src/index.js",
    "start:worker": "node backend/src/worker/worker.js",
    "start": "concurrently \"npm run start:backend\" \"npm run start:worker\"",
    "seed": "npm run seed --prefix backend",
    "test": "npm run test --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
